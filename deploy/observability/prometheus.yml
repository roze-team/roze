global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: roze-gateway
    metrics_path: /metrics
    static_configs:
      - targets:
          - 127.0.0.1:8081
        labels:
          app: roze-gateway
